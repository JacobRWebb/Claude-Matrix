{
  "mcpServers": {
    "matrix": {
      "command": "${PLUGIN_DIR}/scripts/run-mcp.sh",
      "env": {
        "MATRIX_DB": "${HOME}/.claude/matrix/matrix.db",
        "MATRIX_MODELS": "${HOME}/.claude/matrix/models",
        "PLUGIN_DIR": "${PLUGIN_DIR}"
      }
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    }
  }
}
